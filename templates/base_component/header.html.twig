<header>
    <div class="nav-container">
        <a href="/" class="logo">
            🌴 Culture<span>974</span>
        </a>
        
        <nav>
            <ul class="nav-links">
                <li><a href="/evenement">Événements</a></li>
                <li>
                    {% include 'base_component/navbar_dropdown.html.twig' %}
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('admin_inscriptions_index') }}" class="{{ app.request.get('_route') starts with 'admin_inscriptions' ? 'active' : '' }}">Inscription</a></li>
                <li><a href="{{ path('app_category_index') }}" class="{{ app.request.get('_route') starts with 'admin_inscriptions' ? 'active' : '' }}">Catégorie</a></li>
                {% endif %}
            </ul>
        </nav>

        <div class="nav-actions">
            {% if is_granted('ROLE_ADMIN') %}

                <a href="{{ path('app_logout') }}" class="btn btn-secondary">Déconnexion</a>
            {% else %}
                <a href="{{ path('admin_inscriptions_index') }}" class="btn btn-primary">Connexion </a>
            {% endif %}
        </div>
    </div>
</header>
